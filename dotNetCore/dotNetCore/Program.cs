using Agilent.MHDA.Dataset;
using System;

namespace dotNetCore
{
    class Program
    {
        /*
         * 1. NET Framework libraries and succeed at compile time 
         * but the .NET Core application may fail at run time if 
         * any required underlying .NET Framework libraries are missing.
         * */
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
            IColumn col; 

        }
    }
}
