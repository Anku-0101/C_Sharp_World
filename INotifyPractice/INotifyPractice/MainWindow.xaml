<Window x:Class="INotifyPractice.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:INotifyPractice"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Label Content="First Name"/>
        <Label Content="Last Name" Grid.Row="1"/>
        <Label Content="Full Name" Grid.Row="2"/>

        <TextBox x:Name="firstName" Text="{Binding Path=PersonObj.FirstName, Mode=TwoWay}" Grid.Column="1" Width="200"/>
        <TextBox x:Name="lastName" Text="{Binding Path=PersonObj.LastName, Mode=TwoWay}" Grid.Column="1" Grid.Row="1" Width="200"/>
        <TextBox x:Name="fullName" Text="{Binding Path=PersonObj.FullName, Mode=TwoWay}" Grid.Column="1" Grid.Row="2" Width="200"/>
    </Grid>
</Window>
